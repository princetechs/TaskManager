<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing task</h1>

  <%= render "form" , task: @task %>

    <div class="flex space-x-2 mt-4">
      <%= link_to "Show this task" , team_task_path(@team, @task), class: "rounded-lg py-3 px-5 bg-gray-100 font-medium"
        %>
        <%= link_to "Back to tasks" , team_tasks_path(@team), class: "rounded-lg py-3 px-5 bg-gray-100 font-medium" %>
    </div>
</div>